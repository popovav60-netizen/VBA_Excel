===========================================
РЕГУЛЯРНЫЕ ВЫРАЖЕНИЯ (РВ) В .Net (VBA & PS)


     МЕТАСИМВОЛЫ РЕГУЛЯРНЫХ ВЫРАЖЕНИЙ (РВ)

     ^   		Определяет начало входной строки.
				Если установлено свойство Multiline, определяет также позицию сразу после "\n" (новая строка) и "\r" (возврат каретки).

     $   		Определяет конец входной строки.
				Если установлено свойство Multiline, определяет также позицию непосредственно перед "\n" (новая строка) и "\r" (возврат каретки).

     \   		Показывает, что следующий символ является спецсимволом.
				Последовательность "\\" (экранирование) соответствует "\", а последовательность "\(" соответствует "(".

     .   		Определяет любой символ, кроме "\n" (новая строка). Чтобы определить любой символ, включая "\n", следует использовать "[\s\S]".

     (шаблон)  	"захватывающее" подвыражение. нумерация с 1, использование при замене через символ $, например $1, при поиске через \, например \1
				Фиксирует ("захватывает") подбор по шаблону в коллекции SubMatces.
				Чтобы определить сами символы "(" и ")", следует использовать "\(" и "\)".

     (?:шаблон)  Не "захватывающее" подвыражение.
                 Полезно для комбинирования частей шаблона с помощью символа "|" ("или").
                 Например, "госпо(?:дин|жа)" это короче, чем "господин|госпожа".
                 Удобно устанавливать повторители (квантификаторы) на все подвыражение
                 НЕ фиксирует подбор в коллекции SubMatces.

     (?=шаблон)  Позитивная опережающая проверка... Может совпасть справа. совпадает в тех позициях, за которыми следует подвыражение
                 "Windows(?=95|98)" подберёт подстроку "Windows" в строке "Windows95", но не в строке "WindowsNT".
                 Дальнейший подбор начинается немедленно, а не после символов, входящих в скобки.
                 НЕ фиксирует подбор в коллекции SubMatces.

     (?!шаблон)  Негативная опережающая проверка... Не может совпасть справа... совпадает в тех позициях, за которыми не следует подвыражение
                 "Windows(?!95|98)" подберёт подстроку "Windows" в строке "WindowsNT", но не в строке "Windows95".
                 В остальном - полностью аналогично "(?=шаблон)".

     x|y 		Условие "или". "вз|перевод" подберёт "вз" и "перевод". "(вз|пере)вод" подберёт "взвод" и "перевод".

     [abc]   	Набор символов. Находит любой из указанных символов в наборе.

     [^abc]  	Исключающий набор символов. Находит любой из неуказанных символов в наборе.

     [a-z]   	Диапазон символов. Находит любой символ из диапазона.

     [^a-z]  	Исключающий диапазон символов. Находит любой символ, не входящий в диапазон.

     \b 		- Граница слова, т.е. позиция между концом слова и пробелом.
     \B 		- НЕ граница слова.
				В VBA НЕ РАБОТАЕТ НА РУССКИХ СЛОВАХ надо заменять на \s+

     \d 		- цифра. Эквивалентно [0-9].
     \D 		- НЕ цифра. Эквивалентно [^0-9].

     \w 		- любой символ слова, включая символ подчёркивания. Эквивалентно [A-Za-z0-9_].
     \W 		- обратно "\w". Эквивалентно [^A-Za-z0-9_].
				В VBA НЕ РАБОТАЕТ НА РУССКИХ СЛОВАХ надо добавлять [А-Яа-я]

     \s 		- пробельный символ. Эквивалентно [\f\n\r\t\v].
     \S 		- непробельный символ. Эквивалентно [^\f\n\r\t\v].

     \cx 		Управляющий символ. Параметр "х" лежит в диапазоне A-Z или a-z.
				Например, "\cM" определяет Control-M или символ возврата каретки,
				"\cJ" - новая строка, "\cL" - новая страница, "\cI" - символ табуляции, "\cK" - символ вертикальной табуляции.

     \f  		Символ новой страницы. Эквивалент "\x0c" и "\cL".
     \n  		Символ новой строки. Эквивалент "\x0a" и "\cJ".

     \r  		Символ возврата каретки. Эквивалент "\x0d" и "\cM".

     \t  		Символ табуляции. Эквивалент "\x09" и "\cI".

     \v  		Символ вертикальной табуляции. Эквивалент "\x0b" и "\cK".

     \xn 		Шестнадцатеричный ASCII-код символа. Должен содержать ровно два символа.

     \num    	Ссылка на предыдущие зафиксированные (найденные) подстроки-соответствия шаблону (SubMatches).
				"обратная ссылка"
				"num" - положительное целое число, номер соответствия в коллекции SubMatches; отсчёт с единицы.
				Например, "(.)\1" определяет два идентичных символа, следующих друг за другом. Используется непосредственно
				в шаблоне РВ

     \un   		Определяет Unicode-символ по его шестнадцатеричному коду. "n" - четырёхзначный шестнадцатеричный код.

     {n}   		Повторитель (квантификатор) Определяет точное количество символов, стоящих перед ним. "n" - неотрицательное целое число.

     {n,}  		Повторитель (квантификатор) Определяет минимальное количество символов, стоящих перед ним.
				"n" - неотрицательное целое число. "{0,}" эквивалентно "*", а "{1,}" эквивалентно "+".

     {n,m} 		Повторитель (квантификатор) Определяет количество символов, стоящих перед ним (от - до).
				"n" и "m" - неотрицательные целые числа, причём n <= m. "{0,1}" эквивалентно "?".

     *   		Повторитель (квантификатор) Определяет ни одного или несколько символов, стоящих перед ним.
				Эквивалентно {0,}.

     +   		Повторитель (квантификатор) Определяет один или несколько символов, стоящих перед ним.
				Эквивалентно {1,}.

     ?   		Повторитель (квантификатор) Определяет ни одного или один символ, стоящий перед ним.
				Эквивалентно {0,1}.
				Если этот метасимвол идёт непосредственно за конструкциями (*, +, ?, {n}, {n,}, {n,m}), это приводит к некоторому изменению
				алгоритма поиска по заданному шаблону...

     все эти повторители / итераторы / квантификаторы за исключением '?' являются "жадными", т.е.
     это означает, что они захватывают максимально возможное число символов.
     размещение вопросисительного знака '?' после оператора делает его "не жадным", т.е. захватывающим символы не более необходимого минимума.
     пример: b+, как и b*, примененные ко входящей строке "abbbbc" найдут "bbbb", в то время как b+? найдет только "b" (+ - минимум 1), а b*? - вообше пустую строку (* - минимум 0)
     b{2,3} найдет "bbb", в то время как b{2,3}? найдет "bb" (от 2 до 3 - минимум 2)
    
     ВАЖНО!
     в RegExp существует ряд ограничений в синтаксисе регулярных выражений при использовании объекта VBScript.RegExp в VBA.
       отсутствуют указатели на начало и конец текста \A и \Z - вместо этих указателей можно использовать указатель конца текста $
       отсутствует ретроспективная проверка (назад)
           (?<=...) - может совпасть слева
           (?<!...) - не может совпасть слева
           отсутствует ряд модификаторов внутри РВ '(?...)' : i,m,s,g,x,r

Внутри перечня [] символ "-'' может быть использован для определения диапазонов символов, например [a-z] представляет все символы между "a'' и "z'', включительно. 

Если Вам необходимо включить в перечень сам символ "-'', поместите его в начало или конец перечня или предварите '\'. Если Вам необходимо поместить в перечень сам символ ']', поместите его в самое начало или предварите '\'. 

Примеры: 
  [-az]      'a', 'z' и '-' 
  [az-]      'a', 'z' и '-' 
  [a\-z]     'a', 'z' и '-' 
  [a-z]      все 26 малых латинских букв от 'a' до 'z' 
  [\n-\x0D]  #10, #11, #12, #13. 
  [\d-t]     цифра, '-' или 't'. 
  []-a]      символ из диапазона ']'..'a'. 


Метасимволы - обратные ссылки 
Метасимволы от \1 до \9 воспринимаются как обратные ссылки. \<n> совпадает с ранее найденным подвыражением #<n>. 

Примеры: 
  (.)\1+         находит 'aaaa' и 'cc'. 
  (.+)\1+        также находит 'abab' и '123123' 
  (['"]?)(\d+)\1 находит "13" (в дв.кавычках), или '4' (в один.кавычках) или 77 (без кавычек) и т.д. 










	
	
'====================================
' RegExp для VBA

	Dim oRegExp As Object
	Set oRegExp = CreateObject("VBScript.RegExp")
	
	' общие настройки для РВ
    oRegExp.IsGlobal = True			' проверять по всему тексту
    oRegExp.IsIgnoreCase = True		' без учета регистра
    oRegExp.IsMultiLine = True		' многострочный текст
	
	
    ' коды вопросов (приблизительно 98%)
 	oRegExp.Pattern = "(?:000|[a-qsxyz1-9]\d\d)"
	If oRegExp.Test "c12" Then ...
	
	
    ' имена файлов 'r', 'q', 'x' с учетом томов
  	oRegExp.Pattern = "^[rqx](?:\d\d|2f|[3-8]i)(?:0[1-9]|[1-2]\d|3[01])(?:0[1-9]|1[0-2]|[1-9abc][a-qswxyz])[1-9a-z](?:_t(?:0[2-9]|[1-9]\d))?\.docx?$"
	If oRegExp.Test sOnlyFileName Then ...
    
	
	
	' вставка разделителей тысяч
	' цифра '(\d)' (захватывающее подвыражение == $1) 
	' за которой следуют 3 цифры в любом количестве '(?=(\d{3})+' - Позитивная опережающая проверка...
	' за которыми далее не цифры '(?!\d)' - Негативная опережающая проверка...
	' кроме 1-го захватывающего подвыражения - остальные подвыражения - это просто проверки на позиции - они не захватывают
	Debug.Print oRegExp.Replace("абвгде 123456789 абвв абвув 45634 аб в 777  5645678 апр", "$1,")
	' абвгде 123,456,789 абвв абвув 45,634 аб в 777  5,645,678 апр
	


#====================================
# match replace Select-String PS

    # коды вопросов (приблизительно 98%)
	If ("c12" -match "(?:000|[a-qsxyz1-9]\d\d)") {...}
	
	
    # имена файлов 'r', 'q', 'x' с учетом томов
	If (sOnlyFileName -match "^[rqx](?:\d\d|2f|[3-8]i)(?:0[1-9]|[1-2]\d|3[01])(?:0[1-9]|1[0-2]|[1-9abc][a-qswxyz])[1-9a-z](?:_t(?:0[2-9]|[1-9]\d))?\.docx?$") {...}


	# вставка разделителей тысяч
	'абвгде 123456789 абвв абвув 45634 аб в 777  5645678 апр' -replace '(\d)(?=(\d{3})+(?!\d))', '$1,' 
	#абвгде 123,456,789 абвв абвув 45,634 аб в 777  5,645,678 апр



	# match (?-i) включает зависимость от регистра, а (?i) отключает. Например:
	'PowerShell' -match '(?-i)power' -> False
	'PowerShell' -match '(?i)power' -> True


	# про Select-String ====================
	
	# -Encoding "windows-1251"
	# -Encoding 1251
	# -Encoding "OEM"
	# -Encoding "Default"
	# Значение Default соответствует кодировке текущей системной кодовой страницы ANSI

	# -List
	# Возвращает только первое совпадение для каждого из входных файлов. По умолчанию командлет Select-String возвращает объект MatchInfo для каждого найденного совпадения.

	# -Quiet
	# Возвращает логическое значение (true или false) вместо объекта MatchInfo. Значение будет равно "true", если найдено соответствие шаблону, в противном случае — "false".

	# -SimpleMatch
	# Вместо сопоставления по регулярному выражению будет использоваться простое сопоставление. При простом сопоставлении командлет Select-String ищет во входных данных текст, указанный в параметре Pattern. Он не интерпретирует значение параметра Pattern в качестве регулярного выражения.


	# -AllMatches
	# Указывает, что командлет ищет более одного совпадения в каждой строке текста. Без этого параметра Select-Stringнаходит только первое совпадение в каждой строке текста.



#====================================
# .Net Regex для PS

	# поддерживает спецификацию POSIX: (Фридл - Регулярные выражения.3ed.2008.pdf)
	# BRE (basic regular expressions, т. е. «базовые регулярные выражения») и 
	# ERE (extended regular expressions, т. е. «расширенные регулярные выражения»)

	# поддерживает вычитание классов, например
	# классу [[a-z]-[aeiou]] будут соответствовать символы, соответствующие классу [a-z], за исключением
	# символов, соответствующих классу [aeiou], т. е. согласные буквы латинского алфавита.
	
	
	# символьные классы POSIX
	[:alnum:] алфавитные символы и цифровые символы
	[:alpha:] алфавитные символы
	[:blank:] пробел и табуляция
	[:cntrl:] управляющие символы
	[:digit:] цифры
	[:graph:] отображаемые символы (не пробелы, не управляющие символы и т. д.)
	[:lower:] алфавитные символы нижнего регистра
	[:print:] аналог [:graph:], но включает пробел
	[:punct:] знаки препинания
	[:space:] все пропуски ([:blank:], символ новой строки, возврат 	курсора и т. д.)
	[:upper:] алфавитные символы верхнего регистра
	[:xdlgit:] цифры, допустимые в шестнадцатеричных числах	(т. е. 0-9a-fA-F)	
	

	[regex] | Get-Member -Static


# На всякий случай, основные особенности синтаксиса статических методов .NET:
# Во-первых, для вызова статического метода класса используется конструкция [класс]::метод()
# Во-вторых, в отличии от параметров командлетов и функций, для передачи параметров методам .NET они помещаются в скобки сразу за именем метода, и разделяются запятой, например: [класс]::метод(параметр1,параметр2)
# В отличии от “родных” конструкций PowerShell, все .NET сравнения по умолчанию чувствительны к регистру символов.
	
	
	$PatternWordRepeat = '\s+([\wА-Яа-яЁё]+)\s+\1'
	
	$TextIncludeWordRepeat = 'проверка повтора повтора слов в тексте тексте'
	
	
	$Res = [regex]::matches($TextIncludeWordRepeat, $PatternWordRepeat)
	$Res
	$Res.Groups
	$Res | % {$_.groups[0].value}







Любой модификатор может меняться с помощью специальной конструкции {(?...)} внутри регулярного выражения. 
Также, Вы можете присвоить значение соответствующему свойству экземпляра объекта RegExp (например, {ModifierX} для изменения модификатора /x, или ModifierStr для изменения сразу нескольких модификаторов). Значения по умолчанию для новых экземпляров объектов TRegExpr определены в {глобальных константах}, например RegExprModifierX определяет значение по умолчанию для ModifierX. 

i 
  Регистро-независимый режим (по умолчанию использует выбранный в ОС язык по умолчанию)
  
m  
  Воспринимать входной текст как многострочный, при этом метасимволы "^'' и "$'' будут совпадать не только в начале и конце текста в целом, но и в начале и в конце всех имеющихся в тексте строк 
  
s 
  Воспринимать входной текст как одну строку. При этом метасимвол ".'' совпадает с любым символом, если же этот модификатор выключен, то он не совпадает с разделителями строк  
  
g  
  Не стандартный модификатор. Выключая его Вы переключаете все повторители в "не жадный" режим (по умолчанию этот модификатор включен). Т.е. если его отключить, то все '+' работают как '+?', '*' как '*?' и т.д.  
  
x 
  Позволяет форматировать шаблон чтобы обеспечить более легкую читаемость 
  
r 
  Не стандартный модификатор. Если включен, то диапазоны вида а-я включают также букву 'ё', А-Я включают 'Ё', а а-Я включает вообще все русские буквы.  
    
	
